#
# Tests configuration file
#
uddi.enabled=true
uddiNotPair.url=${uddi.url}
wsNotPair.name=${ws.name}

uddiPair.url=
wsPair.name=

# ws url below is used only if uddi is disabled above
wsNotPair.url=${ws.url}

wsPair.url=
